TwitchInputExample:

A basic platformer example, to configure authentication complete the following:

1) secure a token from https://twitchapps.com/tmi
2) paste token into TwitchInputExample_Auth.asset#Token field (without ouath: prefix)
3) set username and channel name on TwitchChatControl#UnityTwitchClient object in the scene
4) run and play!